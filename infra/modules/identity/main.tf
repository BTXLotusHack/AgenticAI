variable "name_prefix" {
  type = string
}

# Cognito is the identity backbone for both planes: it authorizes the API
# Gateway JWT authorizer and AppSync client subscriptions.
resource "aws_cognito_user_pool" "main" {
  name = "${var.name_prefix}-users"

  username_attributes      = ["email"]
  auto_verified_attributes = ["email"]

  password_policy {
    minimum_length    = 8
    require_lowercase = true
    require_numbers   = true
    require_uppercase = true
    require_symbols   = false
  }

  account_recovery_setting {
    recovery_mechanism {
      name     = "verified_email"
      priority = 1
    }
  }
}

# Public client for the React web app and Flutter mobile app (no client secret;
# uses SRP + hosted flows). The API Gateway JWT authorizer validates tokens
# minted for this client (audience check).
resource "aws_cognito_user_pool_client" "user" {
  name         = "${var.name_prefix}-user-client"
  user_pool_id = aws_cognito_user_pool.main.id

  generate_secret = false

  explicit_auth_flows = [
    "ALLOW_USER_SRP_AUTH",
    "ALLOW_REFRESH_TOKEN_AUTH",
  ]

  supported_identity_providers = ["COGNITO"]

  access_token_validity  = 60
  id_token_validity      = 60
  refresh_token_validity = 30
  token_validity_units {
    access_token  = "minutes"
    id_token      = "minutes"
    refresh_token = "days"
  }
}

output "user_pool_id" {
  value = aws_cognito_user_pool.main.id
}

output "user_pool_arn" {
  value = aws_cognito_user_pool.main.arn
}

output "user_client_id" {
  value = aws_cognito_user_pool_client.user.id
}
