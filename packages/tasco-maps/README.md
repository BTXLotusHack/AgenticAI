# @loopin/tasco-maps

Framework-independent Tasco Maps HTTP client with Zod-validated responses.

Use from server-side adapters only. Resolve bearer tokens and API keys through environment-backed header providers; never bundle credentials in browser code.
